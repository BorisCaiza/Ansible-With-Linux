---
- name: Ansible with Linux
  hosts: node1
  gather_facts: false
  become: true
  collections:
    - community.postgresql

  tasks:
    - name: Create Database
      postgresql_db:
        name: mydatabase
        state: present
      become_user: postgres